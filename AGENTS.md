Always run scripts through the project’s required mise runtime.
And remember we are using `vite plus` so instead of `npm run lint` we use `vp lint`, `vp test`, etc.
Ensure you are using the project configured setup before claiming tests are failing.
`
